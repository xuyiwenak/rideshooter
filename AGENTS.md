# Codex 项目指引

作用范围：本仓库及全部子目录。进入子目录修改前，同时阅读沿途更具体的 AGENTS.md。

## 开始任务时

1. 先读根目录 [README.md](README.md)：它是项目目录记忆，包含当前状态、代码入口和文档职责。
2. 按任务定位模块，再读该模块 AGENTS.md；跨模块规则见 [prototype/ARCHITECTURE.md](prototype/ARCHITECTURE.md)。
3. 查看 [doc/README.md](doc/README.md) 的最新版本记录，确认最近改动与未完成事项。历史记录是背景，不替代当前代码或用户本轮要求。
4. 检查工作区已有改动，保留用户工作；不自动提交、覆盖或清理无关文件。

## 固定边界

- 引擎工程在 prototype/，根目录是项目文档，不是 Godot 工程目录。
- 三条水平道路、上下换道、自动骑射；没有左右自由移动。
- main.gd 仅做组装、时钟、输入分发与重开。不要把各模块逻辑重新集中回主脚本。
- 当前是灰盒 Demo。完整 Boss、多路 Boss 技能、局外成长等不能写成已经交付。
- 优先沿用原生 GDScript；引入插件、素材或外部代码时先明确必要性、许可和兼容性。
- 排除 .godot/ 缓存搜索；不要手动修改导入缓存或随意重建 .uid。

## 每次交付后维护记忆

- 仅咨询/只读检查不必新建版本；实际代码、配置、策划或工程文档变更完成后，按 doc/AGENTS.md 新建版本记录。
- 按本地 Asia/Shanghai 日期使用 doc/YYYY-MM-DD/vNNN/。这是当日修改批次编号，不是游戏发行版本。
- 更新 doc/README.md 索引；目录、入口或当前状态改变时同步根 README.md；模块职责改变时同步对应 AGENTS.md 和 ARCHITECTURE.md。
- 记录真实验证命令和结果。沿用历史结果必须明确标注，不能假装本轮重跑。
- 文档记录不代替 Git 代码版本；未经用户请求，不创建提交或代码快照副本。
